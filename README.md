# Hello Bot

A simple Discord bot built with **discord.py** featuring a `/hello` slash command.

## 🚀 Features
- `/hello` — Replies with a friendly greeting
- Slash command support

## 📦 Installation
```
git clone <your repo>
cd hello-bot
pip install -r requirements.txt
```

Create `.env`:
```
BOT_TOKEN=your_token_here
```

Run:
```
python bot.py
```
